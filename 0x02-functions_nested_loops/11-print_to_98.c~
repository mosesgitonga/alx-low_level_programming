#include"main.h"
/**
 * print_to_98 - print num from n 
 * @n: int n
 * Return: sum
 */
void print_to_98(int n);
{
	int x;

	for (x = n; x <= 98; x++)
	{
		_putchar ((x / 10) + '0');
		_putchar ((x % 10) + '0');
		_putchar ((','));
		_putchar ((' '));
		return (x);
	
	}
	return (0);

}
