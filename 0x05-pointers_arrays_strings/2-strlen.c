#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - finds length of a string
 * @s: var
 * Return:length
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
